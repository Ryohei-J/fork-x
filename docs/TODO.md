# TODO

現状はすべて概算・簡略化ロジック。使いながら優先度の高いものから精度を上げていく。

## 計算精度（優先度高いものから）

- [x] フリーランスの必要経費率を入力可能にする（UIから0〜100%で指定可能に。初期値は`DEFAULT_FREELANCE_EXPENSE_RATE`10%）
- [ ] 居住地（都道府県）を計算に反映させるかどうか検討する（参考表示のみの入力項目だったため一旦削除。反映するなら国民健康保険料・住民税・協会けんぽ料率などに自治体ごとの差を組み込む必要がある）
- [ ] 国民健康保険料の自治体ごとの料率テーブルを整備する（自治体ごとの差がかなり大きい）
- [x] 青色申告(65万控除)/白色申告(控除なし)を選択できるようにする（UIにトグルを追加。`TaxFilingType`）
- [ ] 個人事業税を計算に含めるか検討する（業種によって対象か判断が分かれるため保留中）
- [ ] 消費税を考慮するか検討する（現状は免税事業者を前提に未算入。報酬が高い場合は誤差が大きくなる）
- [x] 年齢入力を追加し、介護保険料（40〜64歳の追加負担）を反映する（正社員・フリーランス両方の内訳に「介護保険料」として表示）
- [ ] 健康保険料率を都道府県ごとの協会けんぽ料率に対応させる（現状は全国平均で一律5%）
- [x] 賞与（ボーナス）を年収に一括で含めている簡略計算を見直す（年収を「月給12ヶ月+賞与4ヶ月・年2回」の比率で分解し、健康保険・厚生年金それぞれの標準報酬月額/標準賞与額の上限を反映。実際の月給・賞与の内訳は入力させない仮定ベースの精度向上）
- [ ] 扶養控除・配偶者控除を年齢区分・所得制限に応じて正確に計算する（現状は人数×38万円で一律）
- [ ] 住民税の均等割・所得割を自治体ごとの実際の税率に近づける（現状は全国一律とみなした概算）

## 入力・UI

- [x] 入力値のバリデーション（マイナス値・極端な値・NaNをエラー表示。`src/lib/calculator/validation.ts`）
- [x] 固定値にしている前提条件を画面上に見えるようにする（「この試算の前提条件」セクションを追加）
- [ ] スマホなど狭い画面でのレイアウト確認
- [x] 入力単位（年収／月報酬）を正社員・フリーランスで別々に選べるようにする（`employeeMode`／`freelanceMode`に分割）

## 品質・運用

- [x] 計算ロジック（`src/lib/calculator/`）のユニットテストを追加する（Vitest、`pnpm test`）
- [x] Vercelへのデプロイ設定（GitHub連携済み、`main`へのpushで自動デプロイ。本番: https://fork-x.vercel.app）
- [ ] 制度改正時に `constants.ts` を更新する運用（年度更新のたびに数値を見直す）
- [x] 低所得者向けの住民税非課税措置を反映する（合計所得金額が非課税限度額（全国共通の概算式、単身45万円+扶養1人あたり35万円等）以下なら均等割・所得割とも非課税にする。`calcResidentTax`）
- [x] 国民健康保険の低所得者軽減措置（7割・5割・2割軽減）を反映する（均等割部分に全国共通の軽減判定基準（所得43万円＋α）を適用。`calcReductionRate`、freelance.ts）

## 備考

- 現状の前提条件・簡略化の一覧は [CLAUDE.md](CLAUDE.md) の「計算ロジックの方針」を参照
